{
  "name": "pirate-game-physics",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "host": "vite --host",
    "serve": "vite --host",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test": "vitest",
    "golden-replay": "npm run test -- --run golden-replay",
    "build:wasm": "cd ../shared && mkdir -p build-wasm && cd build-wasm && emcmake cmake -DCMAKE_BUILD_TYPE=Release .. && emmake make pirate-sim.wasm -j$(nproc) && cp pirate-sim.wasm.js ../client/public/wasm/pirate-sim.js && cp pirate-sim.wasm.wasm ../client/public/wasm/pirate-sim.wasm"
  },
  "devDependencies": {
    "@types/node": "^20.5.0",
    "jsdom": "^27.0.0",
    "typescript": "^5.9.2",
    "vite": "^4.4.5",
    "vitest": "^0.34.0"
  }
}
